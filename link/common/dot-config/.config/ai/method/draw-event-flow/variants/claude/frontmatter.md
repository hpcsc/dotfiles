---
name: draw-event-flow
argument-hint: The flow to draw (a module, a feature, a set of reactors) — or a path to notes describing it
description: Draw an event flow diagram — what events a system emits, which automations react to them, and what gates each emission — using Event Modeling's event and automation symbols (orange event pills, bare gear automations) without its full swimlane structure. Produces a standalone SVG and a Mermaid source. Use when asked to diagram an event-driven flow, show which lambda/reactor emits which event, map reactors and their triggers, or visualise a feature toggle or rule gating an emission.
---
