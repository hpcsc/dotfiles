local arguments=$@

local pod=$(kubectl get pod --no-headers | awk '{ print $1 }' | fzf --height 30% --reverse -0 -1 --header "Select pod")
if [ "$pod" = "" ]; then
  return
fi;

cmd="kubectl logs ${pod} ${arguments}"

echo "=== ${cmd}"
eval "${cmd}"
