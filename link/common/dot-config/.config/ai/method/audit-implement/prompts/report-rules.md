Produce `findings`: one entry for every survivor, most severe first. Each entry carries only the survivor's `id`, verbatim from the list above, its `severity` after the re-grade, its `confidence`, and a `note` when you have one to add. `confidence` is "confirmed" when execution reproduced the finding or a quality rule was cited at a specific line, and "plausible" otherwise.
Do not write out a finding's claim, file, line, lens or evidence. clerk joins those onto your grade, and to write them again costs the round minutes of output. clerk drops an id that is not in the list above, and it reports a survivor you leave out at the grade its lens gave.
A survivor marked NOT EXECUTED reached you because nobody could run the check, not because anyone established it: give it `confidence: "plausible"`, and clerk marks its evidence as not executed, so the caller can see the difference between a defect proved and a defect nobody could test.
