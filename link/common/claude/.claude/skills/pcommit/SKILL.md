---
description: Create a git commit for staged changes using the commit agent.
disable-model-invocation: true
---

Delegate to the `commit` agent:

```
Create a git commit for staged changes: $ARGUMENTS
```
